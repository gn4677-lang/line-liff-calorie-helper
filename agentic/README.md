# Agentic Rewrite

This tree is the new mainline for the rewrite.

- `backend/`: Agent Core, API, worker, and tests.
- `frontend/`: LIFF-first Today / Eat / Progress / Settings shell.
- `docs/`: rewrite-specific architecture notes.

Legacy code in the repo root remains available as migration and regression reference only.
