[pytest]
cache_dir = backend/.pytest_cache
addopts = --strict-markers
markers =
    agentic: End-to-end checks for provider wiring, LLM review, fallback paths, async jobs, and observability on agentic surfaces.
